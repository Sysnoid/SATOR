# Aligned with pyproject + CUDA PyTorch; keep torch/cu* line in sync with Dockerfile.cuda
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.6.0
pydantic-settings>=2.2.1
structlog>=24.1.0
prometheus-client>=0.20.0
httpx>=0.27.0
numpy>=1.26.0
scipy>=1.11.0
# PyTorch CUDA wheels (version must match the +cu* suffix). See https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
botorch>=0.10.0
gpytorch>=1.11.0
scikit-learn>=1.3.0

